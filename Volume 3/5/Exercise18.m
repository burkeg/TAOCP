function xs = Exercise18(n,k)
    powers = (1:k).^n;
    for currK = k:-1:2
        currentGoal = powers(currK)
        currSum=0
        bestSoFar = zeros(1,n-1);
        highestEligible = k-1
        for
    end
end
